services:
  - type: web
    name: shenshou
    runtime: static
    buildCommand: npm install
    staticPublishPath: web
    routes:
      - type: rewrite
        source: /api/*
        destination: https://xueshutools.cn/ruyi-api/api/*
      - type: rewrite
        source: /*
        destination: /index.html
